## Multiplicar conse app in node

Esta es una aplicación para generar archivos de tablas de multiplicar al pasar un comando adecuado por consola

Ejecutar el siguiente comando antes de correr el código.

```
npm install
```